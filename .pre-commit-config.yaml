repos:
  - repo: local
    hooks:
      - id: data-lock-coupling
        name: data.lock must update with docs/ changes
        language: system
        entry: node scripts/lib/check-data-lock.mjs
        pass_filenames: false
        stages: [pre-commit]
