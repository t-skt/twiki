repos:
  - repo: local
    hooks:
      - id: tdata-lock-coupling
        name: tdata.lock must update with docs/ changes
        language: system
        entry: node scripts/lib/check-tdata-lock.mjs
        pass_filenames: false
        stages: [pre-commit]
